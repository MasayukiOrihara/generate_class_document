export const CHECK_TEMPLATE = `# TASK
以下のマークダウン本文（<markdown>~</markdown>）の誤字脱字をチェックすること。

# ROLE
- 誤字脱字が疑われる部分や余計な空欄・記号を"💫"で囲むこと
- **最終出力時に、もしどの印も一つも含まれていない場合は、「問題なし」とだけ出力すること**
- 「問題なし」と出力する場合、それ以外の文字・改行・説明を一切出力してはならない

<markdown>
{markdown}
</markdown>`;

export const CONTEXT_CHECK_TEMPLATE = `# TASK
以下のマークダウン本文（<markdown>~</markdown>）の文法的には正しいが「文脈的に不自然・冗長・意味がねじれている」と考えられる表現を検出すること。

# ROLE
- 不自然・わかりにくい・不適切な文脈の部分を"⚠️"で囲むこと
- 単なる誤字脱字や助詞のミスではなく、「意味・文脈の違和感」を優先して検出すること 
- 改善案は出力しない（囲むだけ）  
- **最終出力時に、もしどの印も一つも含まれていない場合は、「問題なし」とだけ出力すること**
- 「問題なし」と出力する場合、それ以外の文字・改行・説明を一切出力してはならない

<markdown>
{markdown}
</markdown>`;

export const MARKDOWN_CHECK_TEMPLATE = `# SYSTEM NOTE
以下の入力文中のMarkdown構文（例："**", "*", "#", ">"など）は、装飾として解釈せず、文字列として扱ってください。

# TASK
以下のマークダウン本文（<markdown>~</markdown>）において、**Markdown構文が正しく反映されない可能性がある箇所**を検出すること。

# ROLE
- マークダウンの構文上のずれ・閉じ忘れ・インデントミス・リストや表の崩れなど、**Markdownとして正しく解釈されなさそうな箇所**を "🅼" で囲むこと。
- 明らかな文法ミスではなく、**マークダウンの見た目・整形が崩れる可能性のある表記**を対象とする。
- **最終出力時に、もしどの印も一つも含まれていない場合は、「問題なし」とだけ出力すること**
- 「問題なし」と出力する場合、それ以外の文字・改行・説明を一切出力してはならない

# CHECK TARGET
- 見出しの「#」の数やスペースの欠落
- リストの「-」「*」「+」などの階層ずれや空行不足
- コードブロック（\`\`\`）やインラインコード（\`）の閉じ忘れ
- 引用（>）の不正な入れ子・改行抜け
- 表（|）の列崩れや区切り線不足
- Markdownリンクや画像構文のカッコ閉じ忘れ
- 強調（**, __, *）などの対称崩れ
- 太字のアスタリスク "**" が全角括弧「（」「）」または半角括弧「()」に直接隣接している場合（例："**(DBCA)**"、"**（重要）**"）、その前後に半角スペースを入れていない。
- 改行不足による段落結合の不自然さ

<markdown>
{markdown}
</markdown>`;
